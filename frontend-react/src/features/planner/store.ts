import { create } from 'zustand';
export type FurnitureType='wardrobe'|'cabinet'|'shelf'|'table'|'sofa';
export type Furniture={id:string;backendId?:number;productId?:number;type:FurnitureType;name:string;width:number;depth:number;height:number;x:number;z:number;rotation:number;texture:string;color:string;drawers:number;handles:number};
export type Room={width:number;length:number;height:number};
type State={projectId:number|null;projectName:string;projectLocation:string;room:Room;items:Furniture[];selected:string|null;step:number;dirty:boolean;setProjectMeta:(patch:Partial<Pick<State,'projectId'|'projectName'|'projectLocation'>>) =>void;setRoom:(room:Room)=>void;replaceProject:(state:{projectId:number;projectName:string;projectLocation:string;room:Room;items:Furniture[]})=>void;add:(type:FurnitureType,patch?:Partial<Furniture>)=>void;select:(id:string|null)=>void;update:(id:string,patch:Partial<Furniture>)=>void;remove:(id:string)=>void;duplicate:(id:string)=>void;markSaved:(projectId:number)=>void};
const presets:Record<FurnitureType,Omit<Furniture,'id'|'type'|'x'|'z'|'rotation'>>={wardrobe:{name:'Шкаф',width:1200,depth:600,height:2100,texture:'wood_dark_oak',color:'',drawers:0,handles:2},cabinet:{name:'Тумба',width:900,depth:450,height:800,texture:'wood_oak',color:'',drawers:2,handles:2},shelf:{name:'Стеллаж',width:1000,depth:350,height:1900,texture:'wood_oak',color:'',drawers:0,handles:0},table:{name:'Стол',width:1400,depth:800,height:760,texture:'countertop',color:'',drawers:0,handles:0},sofa:{name:'Диван',width:2100,depth:900,height:850,texture:'fabric_gray',color:'',drawers:0,handles:0}};
const makeId=()=>crypto.randomUUID();
export const usePlanner=create<State>((set)=>({projectId:null,projectName:'Новый проект',projectLocation:'Онлайн',room:{width:6000,length:5000,height:2800},items:[],selected:null,step:1,dirty:false,setProjectMeta:patch=>set({...patch,dirty:true}),setRoom:room=>set({room,step:2,dirty:true}),replaceProject:value=>set({...value,selected:value.items[0]?.id??null,step:value.items.length?3:2,dirty:false}),add:(type,patch)=>set(s=>{const p={...presets[type],...patch};const item: Furniture={...p,id:makeId(),type,x:patch?.x??s.room.width/2,z:patch?.z??s.room.length/2,rotation:patch?.rotation??0};return{items:[...s.items,item],selected:item.id,step:3,dirty:true}}),select:selected=>set({selected}),update:(id,patch)=>set(s=>({items:s.items.map(x=>x.id===id?{...x,...patch}:x),dirty:true})),remove:id=>set(s=>({items:s.items.filter(x=>x.id!==id),selected:s.selected===id?null:s.selected,dirty:true})),duplicate:id=>set(s=>{const source=s.items.find(x=>x.id===id);if(!source)return s;const copy={...source,id:makeId(),backendId:undefined,name:`${source.name} (копия)`,x:Math.min(source.x+200,s.room.width-source.width/2),z:Math.min(source.z+200,s.room.length-source.depth/2)};return{items:[...s.items,copy],selected:copy.id,dirty:true}}),markSaved:projectId=>set({projectId,dirty:false})}));
